
auth:
  jwt_secret: "sraka"
  root_ca: "ca.crt"


bridge:
  bridge1:
    cidr: "10.8.0.1/16" # Addr here corresponds to gateway(vpn server host), dns server launched here
    mtu: 1400
    dns: "1.1.1.1" # Where to proxy dns requests
    ingresses:
      liberator0:
        type: "liberator"
        listen_addr: ":2100"
        cert: "./certs/node_0.crt"
        key: "./certs/node_0.key"
    egress:
      iface_in_name: "liberator"
      iface_out_name: "amn0"


mesh:
  listen_addr: ":9000"
  bootstrap_addrs:
    - "17.2.2.1:9000"
    - "81.12.66.2:9000"
  peers_store: "peers.json"
  discovery_interval: 30s 
  rtt_update_interval: 10s
  cert: "./certs/node_0.crt"
  key: "./certs/node_0.key"
  root_cert: "./certs/root.crt"


metrics:
  listen_addr: ":9090"